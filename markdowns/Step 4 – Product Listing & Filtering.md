Goal: Create a full product catalogue with sorting, filtering, and pagination.

Tasks:

Build app/products/page.tsx.

Use server‑side searchParams to handle:

category, minPrice, maxPrice, rating, sort (price‑asc/desc, rating).

page and limit for pagination.

Fetch filtered products from API (implement filtering logic in route).

Show number of results and pagination controls.

Add a “Filter” sidebar for mobile (drawer).

Include a “Clear all” button.

Implement ProductGrid component.

Deliverables:

Fully functional product listing with URL‑based filters.

Client‑side state for UI (e.g., active filters) using useSearchParams.