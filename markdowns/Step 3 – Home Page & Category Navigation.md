Goal: Build the landing page with dynamic content.

Tasks:

Create app/page.tsx with sections:

Hero Banner (carousel with promotional images and CTA).

Category Cards (grid of categories with icons/images).

Featured Products (a horizontal scroll or grid).

Special Offer banner (e.g., “Buy 2 Get 1 Free”).

New Arrivals section.

Fetch products with ?featured=true or ?new=true from API.

Implement a ProductCard component (image, name, price, rating, quick‑add button).

Add skeleton loading states.

Deliverables:

A complete, responsive homepage.

Data‑driven categories and products.