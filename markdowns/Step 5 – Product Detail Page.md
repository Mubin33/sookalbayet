Goal: Show detailed product info and handle add‑to‑cart.

Tasks:

Create app/products/[slug]/page.tsx (dynamic route).

Fetch product by slug from API.

Display:

Image gallery (thumbnails, lightbox).

Product name, description, price (including original if discounted).

Rating and reviews count.

Stock status.

Quantity selector (1–10).

“Add to Cart” button (with toast notification).

“Add to Wishlist” button (toggle heart).

Below: similar products or related items (based on category).

Use useRouter to navigate after add‑to‑cart if needed.

Deliverables:

Rich product page.

Add‑to‑cart works and updates global cart state.