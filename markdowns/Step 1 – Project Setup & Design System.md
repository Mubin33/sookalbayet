Goal: Initialize Next.js, Tailwind, and establish the reusable UI foundation.

Tasks:


Install additional packages: zustand (state), react-hook-form (forms), react-hot-toast (notifications)

Set up folder structure:
app/, components/ (ui, layouts, modules), data/, hooks/, lib/, types/

Define global CSS variables for the colour palette (using Tailwind config).

Create layout components: Header, Footer, Sidebar (for mobile filters).

Build atomic UI components: Button, Input, Card, Modal, Spinner, Badge, Rating.

Set up a ThemeProvider (optional for dark/light) but we’ll stick to light.

Create a Container component for consistent max‑width.

Deliverables:

Functional Next.js app with Tailwind.

UI component library ready.

Colour scheme applied to all base elements.